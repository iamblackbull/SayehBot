const { consoleTags } = require("../../utils/main/mainUtils");

module.exports = {
  name: "debug",

  async execute(message) {
    //console.error(`${consoleTags.player} General debug event: `, message);
  },
};
